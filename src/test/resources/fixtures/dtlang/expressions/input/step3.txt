seq(
  let(x, 5),
  let(y[5][type:in.y], 7),
  switch(
    case(123+5 =124, seq(
      return(),
      break()
    )
    case(22+size(x) = 0, continue())
    case(true,
      for(i, 1, 50, seq(
          let(x, x+1)
          switch(
            case(x>10, continue(i)),
            case(x<0, call(doIt))
          )
      )
    )
  )
)